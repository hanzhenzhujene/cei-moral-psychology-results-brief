# Selected-grid source snapshot

These two CSVs are exact copies from the pinned benchmark source repository at `b3a348684692f615d789392692ce34a1359192d3`. Their hashes and source paths are recorded in `SOURCE_MANIFEST.csv`.

They support only the explicitly exploratory size-tier and release-period charts. The table has no saved uncertainty, and none of its 102 successful `.eval` paths resolves in the fresh source clone. Missing or failed rows are never plotted as zero.

Current primary aggregates and saved uncertainty remain under `../canonical-audit/`; that bundle passed tracked-artifact validation but is not a raw-output replay.
